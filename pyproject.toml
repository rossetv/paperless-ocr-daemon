[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "paperless-ocr"
version = "1.0.0"
dependencies = [
    "requests",
    "openai",
    "Pillow",
    "pdf2image",
]

[tool.setuptools.packages.find]
where = ["src"]